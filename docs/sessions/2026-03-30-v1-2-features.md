# 2026-03-30 — v1.2 Features: Launch at Login, Window Position, Trackpad, Bugs

## Session Summary
Added launch at login toggle, bottom-right window positioning, trackpad crop slider support, and fixed 6 bugs/UX issues: SAVE button state, network error copy, art movement in metadata, font mismatch in ZibaLogo, context-aware actions in detail screens.

## What Got Done
- [x] Window opens bottom-right (400×700, 20pt margin) via NSScreen in MainFlutterWindow.swift
- [x] `launch_at_startup` package added; LaunchAtStartup.instance.setup() called in main()
- [x] Launch at login SwitchListTile in Settings > STARTUP section; persisted via SharedPreferences
- [x] Trackpad two-finger horizontal scroll on crop slider (Listener + PointerScrollEvent) in both HomeScreen and ArtworkDetailScreen
- [x] SAVE button fix: `ref.invalidate(isFavoriteProvider)` after addFavorite/removeFavorite (was never refreshing)
- [x] Network error copy: friendly "Couldn't reach WikiArt…" message replaces raw e.toString()
- [x] Art movement (style) now saved to DB on upsert (was missing from ArtworksCompanion.insert)
- [x] Art movement shown in metadata byline: Artist · YYYY · Style (home + detail screens)
- [x] ArtworkData model gains `style` field; fromRow and fromModel updated
- [x] ArtworkDetailScreen gains `showSaveButton` param; History passes true, Saved passes false (default)
- [x] History detail: SAVE (heart toggle) + SET overlay buttons
- [x] Saved detail: SET only (delete button removed)
- [x] ZibaLogo ز font changed from Georgia to .SF Arabic — matches app icon glyph

## Key Files
- `macos/Runner/MainFlutterWindow.swift` — bottom-right positioning via NSScreen
- `lib/main.dart` — LaunchAtStartup.instance.setup()
- `lib/state/app_state.dart` — launchAtLogin setting + upsert fix
- `lib/ui/screens/settings_screen.dart` — STARTUP section
- `lib/ui/screens/home_screen.dart` — SAVE fix, error copy, trackpad, style metadata
- `lib/ui/screens/artwork_detail_screen.dart` — showSaveButton, _SaveOverlayButton, trackpad, style metadata
- `lib/ui/screens/history_screen.dart` — showSaveButton: true
- `lib/models/artwork_data.dart` — style field
- `lib/ui/widgets/ziba_logo.dart` — .SF Arabic font

## TODO (Next Session)
- [ ] Sign, notarise, staple → v1.2.0 release
- [ ] Update Homebrew formula SHA256
- [ ] Crop slider trackpad feel tuning (divisor 600 — adjust if too fast/slow)
- [ ] Test launch at login on clean boot
- [ ] Art movement filter in Settings still not wired to WikiArt query
- [ ] ز logo widget: verify .SF Arabic glyph matches icon at small sizes
- [ ] Dark/Light/System theme toggle
- [ ] Menubar app + launch at login (next iteration)
